# ═══════════════════════════════════════════════════════════════════════════
# k8s/gcp/08-passengers.yaml — Passengers Microservice (Acceso Público LoadBalancer)
# ═══════════════════════════════════════════════════════════════════════════
apiVersion: apps/v1
kind: Deployment
metadata:
  name: passengers
  namespace: apps
  labels:
    app: passengers
    service: passengers-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: passengers
  template:
    metadata:
      labels:
        app: passengers
        service: passengers-service
    spec:
      containers:
        - name: passengers
          image: us-central1-docker.pkg.dev/GCP_PROJECT_ID_PLACEHOLDER/actividad-3-3/passengers:latest
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: "Production"
            - name: ASPNETCORE_URLS
              value: "http://+:8080"
            - name: Services__DataAccessUrl
              value: "http://data-access.apps.svc.cluster.local:5003"
            - name: OpenTelemetry__OtlpEndpoint
              value: "http://otel-collector.observability.svc.cluster.local:4317"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://otel-collector.observability.svc.cluster.local:4317"
            - name: OTEL_SERVICE_NAME
              value: "passengers-service"
          ports:
            - containerPort: 8080
              name: http
          resources:
            requests:
              cpu: 100m
              memory: 200Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 20

---
# ── Acceso público (LoadBalancer)
apiVersion: v1
kind: Service
metadata:
  name: passengers
  namespace: apps
  labels:
    app: passengers
spec:
  selector:
    app: passengers
  ports:
    - name: http
      port: 5001
      targetPort: 8080
  type: LoadBalancer
